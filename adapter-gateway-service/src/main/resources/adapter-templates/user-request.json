{
  "requestId": "${correlationId}",
  "timestamp": "${currentTimeISO}",
  "system": "${systemName}",
  <#if userId??>
  "userId": "${userId}",
  </#if>
  "metadata": {
    "version": "${version}",
    "source": "adapter-service"
  }
}

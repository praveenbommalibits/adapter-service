# Project version (can be overridden by -Pversion command line argument)
version=4.0.1-SNAPSHOT

# Gradle configuration
#org.gradle.jvmargs=-Xmx2048m -XX:MaxPermSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Publishing configuration (optional - for local development)
# These will be overridden by GitHub Actions environment variables
# gpr.user=your-github-username
# gpr.key=your-personal-access-token
gpr.repository=praveenbommalibits/adapter-service
